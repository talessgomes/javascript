// Setup
const myArr = [2, 3, 4, 5, 6];

// Only change code below this line
let total = 0;

for (let i = 0; i < myArr.length; i++) {
    total += myArr[i];
}

/*outra forma de resolver
  total = myArr.length * i;
   */

  /*O looping vai multiplicar a quantidade de número que tem 
dentro do array pela variável i e vai jogar o valor */